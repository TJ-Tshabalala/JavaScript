// Practice exerice using ternary operator

let id = true; // define a var 'id' with an initial bool value of 'true'

// define a var 'message'
// use ternary operator to check if id is true
// if id is true, assign message with the value of 'allowed', else if id is false, assign message with the value 'denied'
let message= id == true ? "allowed": "denied";


// Output the message value based on the ternary operator condition statement.
console.log(message);