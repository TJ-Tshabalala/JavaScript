let shoppingList = ["Milk", "Bread","Apples"]; // Define an array called shoppingList and initialize it with values
console.log(shoppingList.length);  // Get length of the array
shoppingList[1] = "Bananas";  // Update the value at index 1  of the shoppingList Array to Bananas
console.log(shoppingList);   // Display the array in the console.