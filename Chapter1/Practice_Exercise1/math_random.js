console.log(Math.random()); // Provides a random number between 0 and 1
console.log(Math.random() * 100); // Provides a random number 0 and 1, but multiplied by 100

console.log(Math.floor(Math.random() * 100)); // Math.floor() is used to round the number down to the nearest integer