const pi = Math.PI;
console.log(pi);

let x = 5.7;

let ceilX = Math.ceil(x);
let floorX = Math.floor(x);

console.log(ceilX);
console.log(floorX);
let randomNumber = Math.floor(Math.random()* 10);
let randomNum = Math.floor(Math.random() * 100);
let i = 0;
do{
    getMax = (x) => {

    let cheese = console.log('Max', Math.max(x));
    }
}
while(i<100, i++)

getMax(randomNumber);